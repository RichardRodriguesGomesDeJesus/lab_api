import request from "supertest";
import app from "../../app";
import { AppDataSource } from "../../datasource";

beforeAll(async () => {
});

afterAll(async () => {
});

describe("Amostra endpoints", () => {
  it("should create an amostra (mock)", async () => {
  });
});